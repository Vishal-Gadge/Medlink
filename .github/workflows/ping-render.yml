name: Ping Render to keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: |
          curl -s -o /dev/null -w "Status: %{http_code}\n" https://medlink-fili.onrender.com/ping